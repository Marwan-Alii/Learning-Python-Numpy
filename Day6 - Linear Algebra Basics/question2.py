# QUESTION 2 - DOT PRODUCT

import numpy as np

weights = np.array([0.2,0.5,0.3])
inputs = np.array([10,4,6])

print("Dot Product:", np.dot(weights, inputs))

# In Neural Networks, dot product is used to find the weighted combination of the inputs
